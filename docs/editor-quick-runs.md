# Editor Quick Runs

The VS Code extension adds a **“▶ Run”** gutter button next to every `defineSchema()` call.

1. Click **Run** → pick a provider & model.
2. Responses stream in the inline terminal.
3. Output is typed to match your schema.

> **TODO:** GIF demo and extension marketplace link.
